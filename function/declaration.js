//Function declarations define named functions that can be called anywhere in the code due to hoisting.

function greet(name) {
    console.log("Hello, " + name + "!");
}

greet("Alice");  // Outputs: Hello, Alice!
