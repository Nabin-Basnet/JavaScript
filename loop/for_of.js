// The for...of loop is used to iterate over iterable objects such as arrays, strings, maps, sets, and other iterable collections. 
// It iterates over the values of the iterable rather than the keys or indices.

//use with array
// let numbers = [1, 2, 3, 4, 5];

// for (let number of numbers) {
//     console.log(number);  // Outputs 1, 2, 3, 4, 5
// }


//used wit string
let str="hello world!";
for(let index of str){
    console.log(index);
}
