//A set is a collection of unique values. Sets are useful for storing values without duplicates.
let set = new Set([1, 2, 3, 3, 4]);

console.log(set.has(3));  // true
console.log(set.size);  // 4

// Add a value
